[package]
name = "fledge-corvid-agent"
version = "0.1.0"
edition = "2021"
description = "Fledge plugin for interacting with a CorvidAgent server"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
